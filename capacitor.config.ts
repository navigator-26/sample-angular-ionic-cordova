import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sample-angular-ionic-cordova',
  webDir: 'www'
};

export default config;
